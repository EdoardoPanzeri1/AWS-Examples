AWSTemplateFormatVersion: "2010-09-09"
Description: A simple s3 bucket
Resources:
  S3Bucket:
    Type: 'AWS::S3::Bucket'
    # We don't need supply a Bucket name as aws will generate a random one for us